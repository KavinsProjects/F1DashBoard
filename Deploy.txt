

base : "/your repo-name/",
Add this in your vite.config.js

npm install gh-pages --save-dev

//package.json
"homepage": "https://KavinsProjects.github.io/PITWALL"

add under the package.json -> under scripts 
"predeploy": "npm run build",
"deploy": "gh-pages -d dist"

commit the changes 

run 
npm run deploy